apiVersion: batch/v1
kind: Job
metadata:
  name: db-migrate-comments
  namespace: go-app
  labels:
    app: db-migrate
    target: comments
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app: db-migrate
        target: comments
    spec:
      restartPolicy: Never
      containers:
        - name: db-migrate-comments
          image: xomrkob/db-migrate:latest
          imagePullPolicy: Always
          args: ["-target=comments"]
          envFrom:
            - configMapRef:
                name: comments-service-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: go-app-secret
                  key: DB_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: go-app-secret
                  key: DB_PASS