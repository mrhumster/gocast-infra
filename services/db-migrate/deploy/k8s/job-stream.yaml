apiVersion: batch/v1
kind: Job
metadata:
  name: db-migrate-stream
  namespace: go-app
  labels:
    app: db-migrate
    target: stream
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app: db-migrate
        target: stream
    spec:
      restartPolicy: Never
      containers:
        - name: db-migrate-stream
          image: xomrkob/db-migrate:latest
          imagePullPolicy: Always
          args: ["-target=stream"]
          envFrom:
            - configMapRef:
                name: stream-service-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: go-app-secret
                  key: DB_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: go-app-secret
                  key: DB_PASS